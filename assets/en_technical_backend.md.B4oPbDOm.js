import{_ as s,o as i,c as a,a2 as t}from"./chunks/framework.Crpw6aS5.js";const k=JSON.parse('{"title":"Backend Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"en/technical/backend.md","filePath":"en/technical/backend.md"}'),n={name:"en/technical/backend.md"};function d(o,e,r,c,l,p){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="backend-architecture" tabindex="-1">Backend Architecture <a class="header-anchor" href="#backend-architecture" aria-label="Permalink to &quot;Backend Architecture&quot;">​</a></h1><h2 id="stack" tabindex="-1">Stack <a class="header-anchor" href="#stack" aria-label="Permalink to &quot;Stack&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Library</th><th>Role</th></tr></thead><tbody><tr><td><code>fastify</code></td><td>HTTP framework, performant and TypeScript-first</td></tr><tr><td><code>@fastify/jwt</code></td><td>JWT auth (access token on every request)</td></tr><tr><td><code>@fastify/cors</code></td><td>CORS configured for <code>FRONTEND_URL</code> only</td></tr><tr><td><code>@fastify/swagger</code></td><td>Automatic OpenAPI generation</td></tr><tr><td><code>@fastify/rate-limit</code></td><td>100 req/min per IP by default</td></tr><tr><td><code>googleapis</code></td><td>Official Gmail API v1 client</td></tr><tr><td><code>bullmq</code></td><td>Async job queue (Redis-backed)</td></tr><tr><td><code>postgres</code></td><td>Lightweight typed PostgreSQL client</td></tr><tr><td><code>ioredis</code></td><td>Redis client (BullMQ + cache)</td></tr><tr><td><code>zod</code></td><td>Input validation</td></tr><tr><td><code>mailparser</code></td><td>EML parsing for reading archives</td></tr><tr><td><code>bcrypt</code></td><td>Password hashing</td></tr><tr><td><code>otplib</code></td><td>TOTP code generation and verification (2FA)</td></tr><tr><td><code>qrcode</code></td><td>QR code generation for 2FA setup</td></tr></tbody></table><hr><h2 id="fastify-plugin-structure" tabindex="-1">Fastify Plugin Structure <a class="header-anchor" href="#fastify-plugin-structure" aria-label="Permalink to &quot;Fastify Plugin Structure&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/plugins/</span></span>
<span class="line"><span>├── index.ts      ← Registration of all plugins (order matters)</span></span>
<span class="line"><span>├── db.ts         ← PostgreSQL connection, app.db decorator</span></span>
<span class="line"><span>└── redis.ts      ← Redis connection, app.redis decorator</span></span></code></pre></div><p>The <code>app.authenticate</code> decorator is defined in <code>plugins/index.ts</code> and used as a <code>preHandler</code> on all protected routes:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">app.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/route-protegee&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { preHandler: [app.authenticate] }, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">req</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">sub</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">userId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">role</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> req.user </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">sub</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">role</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p>Two additional decorators ensure multi-user isolation:</p><ul><li><strong><code>app.requireAccountOwnership</code></strong> — Verifies that the route&#39;s <code>:accountId</code> parameter belongs to the authenticated user (lookup in <code>gmail_accounts</code>). Used on all Gmail, archive, dashboard, and rules routes.</li><li><strong><code>app.requireAdmin</code></strong> — Verifies that <code>role === &#39;admin&#39;</code> in the JWT payload. Used on all <code>/api/admin/*</code> routes.</li></ul><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Example: route isolated by account</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">app.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/mails/:accountId&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  preHandler: [app.authenticate, app.requireAccountOwnership]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}, handler)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Example: admin route</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">app.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/admin/users&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  preHandler: [app.authenticate, app.requireAdmin]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}, handler)</span></span></code></pre></div><hr><h2 id="oauth2-token-management" tabindex="-1">OAuth2 Token Management <a class="header-anchor" href="#oauth2-token-management" aria-label="Permalink to &quot;OAuth2 Token Management&quot;">​</a></h2><p>Two Google OAuth2 flows coexist:</p><ol><li><strong>Gmail OAuth2</strong> — To connect a Gmail account (scopes <code>gmail.modify</code>, <code>gmail.labels</code>, <code>userinfo.email</code>). Tokens are stored encrypted in <code>gmail_accounts</code>.</li><li><strong>Google SSO</strong> — For user authentication (scopes <code>openid</code>, <code>userinfo.email</code>, <code>userinfo.profile</code>). Creates or merges a user account based on the <code>google_id</code>. The dedicated redirect URI is configured via <code>GOOGLE_SSO_REDIRECT_URI</code>.</li></ol><p>Google tokens are stored encrypted in the database (<code>gmail_accounts</code> table). Refresh is automatic via the Google client <code>tokens</code> event:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">oauth2Client.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">on</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;tokens&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">tokens</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Mise à jour automatique access_token + expiry en base</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">Refresh token</p><p>Google only returns the <code>refresh_token</code> on the <strong>first</strong> consent (<code>prompt: &#39;consent&#39;</code>). If a user reconnects the same account, the previous refresh_token is preserved via <code>COALESCE</code> in SQL. Never overwrite an existing refresh_token with <code>null</code>.</p></div><p>Google SSO uses <code>prompt: &#39;select_account&#39;</code> to allow the user to choose their account without re-requesting consent on every login.</p><hr><h2 id="gmail-api-throttling" tabindex="-1">Gmail API Throttling <a class="header-anchor" href="#gmail-api-throttling" aria-label="Permalink to &quot;Gmail API Throttling&quot;">​</a></h2><p>The Gmail API quota is <strong>250 units/user/second</strong>. Each <code>messages.get</code> costs 5 units.</p><p>The conservative configuration adopted:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">GMAIL_BATCH_SIZE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 100 messages.get en parallèle = 500 unités</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">GMAIL_THROTTLE_MS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Pause 500ms entre chaque batch → ~1000 unités/sec max</span></span></code></pre></div><p>With throttling, we stay at ~1,000 units/sec which exceeds the quota if multiple users are active simultaneously. In production with multiple accounts, reduce <code>GMAIL_BATCH_SIZE</code> to 50 or increase <code>GMAIL_THROTTLE_MS</code> to 1000ms.</p><hr><h2 id="unified-worker-startup" tabindex="-1">Unified Worker Startup <a class="header-anchor" href="#unified-worker-startup" aria-label="Permalink to &quot;Unified Worker Startup&quot;">​</a></h2><p>A <strong>unified worker</strong> (<code>unified.worker.ts</code>) listens on the <code>nid</code> queue and dispatches each job by <code>job.name</code> via a <code>switch</code>. This prevents multiple workers listening on the same queue from stealing each other&#39;s jobs.</p><p>Each worker injects the <code>user_id</code> into the <code>jobs</code> table to ensure per-user data isolation.</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { startUnifiedWorker } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./jobs/workers/unified.worker&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Dans bootstrap()</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">startUnifiedWorker</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span></code></pre></div><p>Job types handled: <code>bulk_operation</code>, <code>archive_mails</code>, <code>run_rule</code>, <code>scan_unsubscribe</code>, <code>scan_tracking</code>, <code>scan_pii</code>, <code>encrypt_archives</code>.</p><hr><h2 id="privacy-security-module" tabindex="-1">Privacy &amp; Security Module <a class="header-anchor" href="#privacy-security-module" aria-label="Permalink to &quot;Privacy &amp; Security Module&quot;">​</a></h2><p>The privacy module (<code>src/privacy/</code>) contains three independent services:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/privacy/</span></span>
<span class="line"><span>├── tracking.service.ts    ← Détection de pixels espions</span></span>
<span class="line"><span>├── pii.service.ts         ← Scanner de données sensibles (PII)</span></span>
<span class="line"><span>└── encryption.service.ts  ← Chiffrement AES-256-GCM des archives</span></span></code></pre></div><h3 id="tracking-pixel-detector" tabindex="-1">Tracking Pixel Detector <a class="header-anchor" href="#tracking-pixel-detector" aria-label="Permalink to &quot;Tracking Pixel Detector&quot;">​</a></h3><p>The service analyzes the HTML body of Gmail messages to identify three types of trackers:</p><ol><li><strong>1×1 pixels</strong> — images with <code>width=1 height=1</code>, <code>display:none</code> or <code>visibility:hidden</code></li><li><strong>Known domains</strong> — database of 35+ ESP domains (Mailchimp, SendGrid, HubSpot, Klaviyo, Brevo…)</li><li><strong>UTM parameters</strong> — links containing <code>utm_source</code>, <code>utm_medium</code>, <code>utm_campaign</code>, etc.</li></ol><p>The scan is launched as an async job (BullMQ). Results are stored in <code>tracking_pixels</code> with JSON details of each detected tracker.</p><h3 id="pii-scanner" tabindex="-1">PII Scanner <a class="header-anchor" href="#pii-scanner" aria-label="Permalink to &quot;PII Scanner&quot;">​</a></h3><p>The service scans archived EML files on disk to detect sensitive data via regex:</p><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>credit_card</code></td><td>Visa, Mastercard, Amex card (with separators)</td></tr><tr><td><code>iban</code></td><td>International IBAN number</td></tr><tr><td><code>french_ssn</code></td><td>French social security number</td></tr><tr><td><code>password_plain</code></td><td>Plaintext password (<code>password:</code>, <code>mdp=</code>, etc.)</td></tr><tr><td><code>phone_fr</code></td><td>French phone number (+33 / 06…)</td></tr></tbody></table><p>Stored snippets are automatically masked (e.g., <code>****-****-****-4242</code>) to avoid exposing actual data.</p><h3 id="archive-encryption" tabindex="-1">Archive Encryption <a class="header-anchor" href="#archive-encryption" aria-label="Permalink to &quot;Archive Encryption&quot;">​</a></h3><p>Encryption uses native Node.js <code>crypto</code>, with no external dependency:</p><ul><li><strong>Algorithm</strong>: AES-256-GCM (confidentiality + integrity)</li><li><strong>Key derivation</strong>: PBKDF2 (SHA-512, 100,000 iterations, random 32-byte salt)</li><li><strong>Storage</strong>: only a scrypt hash of the passphrase is stored in the database (<code>users.encryption_key_hash</code>), never the passphrase itself</li><li><strong>Idempotency</strong>: already encrypted files are detected by magic bytes <code>GMENC01</code> and skipped</li><li><strong>On-the-fly decryption</strong>: via the <code>decrypt-mail</code> endpoint, the file remains encrypted on disk</li></ul><hr><h2 id="eml-and-mime-decoding" tabindex="-1">EML and MIME Decoding <a class="header-anchor" href="#eml-and-mime-decoding" aria-label="Permalink to &quot;EML and MIME Decoding&quot;">​</a></h2><p>The archiving service retrieves emails in <code>raw</code> format (complete EML encoded in URL-safe base64). Since this format doesn&#39;t populate <code>payload.headers</code>, headers (Subject, From, To, Date) are parsed directly from the raw EML content.</p><p>Subjects using RFC 2047 encoding (<code>=?UTF-8?B?...?=</code>, <code>=?UTF-8?Q?...?=</code>) are decoded by the <code>decodeMimeWords()</code> function which handles:</p><ul><li><strong>Base64</strong> (<code>?B?</code>) — standard decoding</li><li><strong>Quoted-Printable</strong> (<code>?Q?</code>) — replacement of <code>=XX</code> and <code>_</code> (space)</li><li><strong>Multiple charsets</strong> — via <code>TextDecoder</code> (UTF-8, ISO-8859-1, etc.)</li></ul>`,51)])])}const g=s(n,[["render",d]]);export{k as __pageData,g as default};
