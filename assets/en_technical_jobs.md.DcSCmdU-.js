import{_ as a,o as i,c as t,a2 as s}from"./chunks/framework.Crpw6aS5.js";const u=JSON.parse('{"title":"Jobs & Queue (BullMQ)","description":"","frontmatter":{},"headers":[],"relativePath":"en/technical/jobs.md","filePath":"en/technical/jobs.md"}'),n={name:"en/technical/jobs.md"};function o(r,e,d,l,c,h){return i(),t("div",null,[...e[0]||(e[0]=[s(`<h1 id="jobs-queue-bullmq" tabindex="-1">Jobs &amp; Queue (BullMQ) <a class="header-anchor" href="#jobs-queue-bullmq" aria-label="Permalink to &quot;Jobs &amp; Queue (BullMQ)&quot;">​</a></h1><h2 id="why-bullmq" tabindex="-1">Why BullMQ <a class="header-anchor" href="#why-bullmq" aria-label="Permalink to &quot;Why BullMQ&quot;">​</a></h2><p>Operations on Gmail (bulk delete, archiving 5,000 emails) can take several minutes. A synchronous HTTP request would inevitably time out.</p><p>BullMQ allows:</p><ul><li>Enqueue the job → immediate <code>202 Accepted</code> response with a <code>jobId</code></li><li>Asynchronous execution in a worker</li><li>Frontend polling on <code>/api/jobs/:id</code> for progress</li><li>Automatic retry on error (exponential backoff, 3 attempts)</li><li>Cancellation of a running job</li></ul><hr><h2 id="job-types" tabindex="-1">Job Types <a class="header-anchor" href="#job-types" aria-label="Permalink to &quot;Job Types&quot;">​</a></h2><p>All job types are handled by a <strong>unified worker</strong> (<code>unified.worker.ts</code>) that dispatches by <code>job.name</code>:</p><table tabindex="0"><thead><tr><th>Type</th><th>Trigger</th></tr></thead><tbody><tr><td><code>bulk_operation</code></td><td>POST <code>/api/gmail/:id/messages/bulk</code></td></tr><tr><td><code>archive_mails</code></td><td>POST <code>/api/archive/:id/archive</code></td></tr><tr><td><code>run_rule</code></td><td>Manual or cron (from the Rules page)</td></tr><tr><td><code>scan_unsubscribe</code></td><td>POST <code>/api/unsubscribe/:id/scan</code></td></tr><tr><td><code>scan_tracking</code></td><td>POST <code>/api/privacy/:id/tracking/scan</code></td></tr><tr><td><code>scan_pii</code></td><td>POST <code>/api/privacy/:id/pii/scan</code></td></tr><tr><td><code>encrypt_archives</code></td><td>POST <code>/api/privacy/:id/encryption/encrypt</code></td></tr><tr><td><code>import_mbox</code></td><td>POST <code>/api/import/:id/mbox</code></td></tr><tr><td><code>import_imap</code></td><td>POST <code>/api/import/:id/imap</code></td></tr><tr><td><code>apply_retention</code></td><td>POST <code>/api/retention/run</code></td></tr><tr><td><code>purge_archive_trash</code></td><td>Scheduled daily job (<code>scheduler.ts</code>, 4 AM)</td></tr></tbody></table><hr><h2 id="queue-configuration" tabindex="-1">Queue Configuration <a class="header-anchor" href="#queue-configuration" aria-label="Permalink to &quot;Queue Configuration&quot;">​</a></h2><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Queue partagée &quot;nid&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defaultJobOptions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  attempts</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,                              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 3 tentatives en cas d&#39;échec</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  backoff</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;exponential&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">delay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 2s, 4s, 8s</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  removeOnComplete</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">count</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Garder les 100 derniers jobs complétés</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  removeOnFail</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">count</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },             </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Garder les 50 derniers jobs en erreur</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><hr><h2 id="concurrency" tabindex="-1">Concurrency <a class="header-anchor" href="#concurrency" aria-label="Permalink to &quot;Concurrency&quot;">​</a></h2><p>The unified worker is configured with a concurrency of <strong>3</strong> (3 simultaneous jobs max). All job types share this concurrency.</p><hr><h2 id="database-tracking" tabindex="-1">Database Tracking <a class="header-anchor" href="#database-tracking" aria-label="Permalink to &quot;Database Tracking&quot;">​</a></h2><p>Each job is tracked in the <code>jobs</code> table (PostgreSQL) for:</p><ul><li>Persistence after restart (BullMQ Redis can be flushed)</li><li>History access even after <code>removeOnComplete</code></li><li>Frontend display without direct Redis dependency</li></ul><h3 id="pre-insertion" tabindex="-1">Pre-insertion <a class="header-anchor" href="#pre-insertion" aria-label="Permalink to &quot;Pre-insertion&quot;">​</a></h3><p>The <code>enqueueJob()</code> function immediately inserts a row in the <code>jobs</code> table with <code>status: &#39;pending&#39;</code> as soon as the job is added to BullMQ. This ensures the frontend (SSE) can find the job in the database without delay, before the worker even picks it up.</p><p>Workers then perform an <code>UPDATE</code> (not an <code>INSERT</code>) to move the job to <code>active</code>, then <code>completed</code> or <code>failed</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>pending → active → completed</span></span>
<span class="line"><span>                → failed</span></span>
<span class="line"><span>         → cancelled (annulation manuelle)</span></span></code></pre></div><hr><h2 id="real-time-tracking-sse" tabindex="-1">Real-Time Tracking (SSE) <a class="header-anchor" href="#real-time-tracking-sse" aria-label="Permalink to &quot;Real-Time Tracking (SSE)&quot;">​</a></h2><p>Progress tracking uses <strong>Server-Sent Events</strong> via the <code>GET /api/jobs/events</code> endpoint:</p><ul><li>The <code>useJobSSE</code> hook opens a persistent SSE connection</li><li>Each event contains <code>{ jobId, status, progress, processed, total }</code></li><li>The SSE broadcaster queries the database (via <code>bullmq_id</code>) on each progress event to retrieve the full job state</li><li>The connection automatically reconnects on disconnection</li><li>A <code>JobProgressModal</code> displays the progress bar in real time</li><li>The <code>NotificationBell</code> component also receives events for toast notifications</li></ul><hr><h2 id="scheduler" tabindex="-1">Scheduler <a class="header-anchor" href="#scheduler" aria-label="Permalink to &quot;Scheduler&quot;">​</a></h2><p>The <code>scheduler.ts</code> file performs periodic checks (every 60 seconds) and enqueues scheduled jobs:</p><table tabindex="0"><thead><tr><th>Job</th><th>Frequency</th><th>Description</th></tr></thead><tbody><tr><td><code>apply_retention</code></td><td>Daily (3 AM)</td><td>Applies active retention policies</td></tr><tr><td><code>purge_archive_trash</code></td><td>Daily (4 AM)</td><td>Permanently deletes expired trashed archives</td></tr></tbody></table><h3 id="archive-trash-purge" tabindex="-1">Archive Trash Purge <a class="header-anchor" href="#archive-trash-purge" aria-label="Permalink to &quot;Archive Trash Purge&quot;">​</a></h3><p>The <code>purge_archive_trash</code> job:</p><ol><li>Reads configuration from the <code>system_config</code> table (<code>archive_trash_retention_days</code>, <code>archive_trash_purge_enabled</code>)</li><li>If disabled (<code>purge_enabled = false</code>), the job completes without action</li><li>Finds archived mails where <code>deleted_at &lt; now - retention_days</code></li><li>Deletes associated files (EML + attachments) from storage</li><li>Deletes the database entries</li></ol><p>Default retention is <strong>30 days</strong>. It can be configured via the UI (Jobs page → Archive trash configuration) or directly in the <code>system_config</code> table.</p>`,35)])])}const k=a(n,[["render",o]]);export{u as __pageData,k as default};
